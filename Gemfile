source "https://rubygems.org"
gemspec

gem 'jekyll-compose', group: [:jekyll_plugins]

# Ruby 3.4 dropped these from the default gems; Jekyll/Liquid still require
# them at runtime, so they need to be explicit dependencies now.
gem 'csv'
gem 'logger'
gem 'bigdecimal'
gem 'base64'
